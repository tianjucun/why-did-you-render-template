# pnpm workspace 配置
# 将依赖提升到顶层 node_modules，避免重复安装

# ========== 方案一：提升所有依赖（简单但可能不够精确）==========
# 如果顶层有包了并且跟子包的版本一致，那就不再安装了
# 类似 npm/yarn 的行为，所有依赖都提升到顶层
shamefully-hoist=true

# ========== 方案二：精确提升特定包（推荐，更节省空间）==========
# 只提升特定的包到顶层，其他包保持隔离
# 取消上面的 shamefully-hoist=true，使用下面的配置：
# shamefully-hoist=false
# public-hoist-pattern[]=@welldone-software/why-did-you-render
# public-hoist-pattern[]=react
# public-hoist-pattern[]=react-dom

# ========== 其他配置 ==========

# 自动安装 peer dependencies
auto-install-peers=true

# 严格 peer dependencies
# false: 允许版本不匹配，只警告（推荐）
# true: 版本不匹配时报错
strict-peer-dependencies=false

# 提升 peer dependencies（可选）
# hoist-pattern[]=*eslint*
# hoist-pattern[]=*prettier*
